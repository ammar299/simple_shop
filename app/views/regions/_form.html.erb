<%= form_with(model: region, local: true) do |form| %>
  <% if region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(region.errors.count, "error") %> prohibited this region from being saved:</h2>

      <ul>
        <% region.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :country_details %>
    <%= form.text_field :country_details %>
  </div>

  <div class="field">
    <%= form.label :currency_details %>
    <%= form.text_field :currency_details %>
  </div>

  <div class="field">
    <%= form.label :tax_details %>
    <%= form.text_field :tax_details %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
